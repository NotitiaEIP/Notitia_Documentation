[build]
  command = "VITE_NOTITIA_API=$Notitia_API npm run build"
  publish = "dist"

# SPA — toutes les routes renvoient vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
